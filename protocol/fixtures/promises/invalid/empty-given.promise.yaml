apiVersion: 1
id: harness.fixture.empty_given
feature: Harness Fixtures / Promise
title: Empty Given is invalid
purpose: Exercise the protocol minItems rule.
priority: P1
boundary: unit
lifecycle: accepted
given: []
when:
  - A Harness implementation decodes the fixture
then:
  - The fixture is rejected
observes:
  - protocol/fixtures/promises/invalid/empty-given.promise.yaml
failureMeaning: Empty Given lists should not be accepted.
review:
  approvedBy: xinyao
