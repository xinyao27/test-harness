# Vite+ Monorepo Starter

这是一个用于创建 Vite+ monorepo 的 starter。

## Development

- 检查项目是否准备就绪：

```bash
vp run ready
```

- 运行测试：

```bash
vp run -r test
```

- 构建 monorepo：

```bash
vp run -r build
```

- 启动开发服务：

```bash
vp run dev
```
